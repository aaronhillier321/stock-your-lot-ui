apiVersion: v1
kind: Service
metadata:
  name: stock-your-lot
  labels:
    app: stock-your-lot
spec:
  type: LoadBalancer
  selector:
    app: stock-your-lot
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
